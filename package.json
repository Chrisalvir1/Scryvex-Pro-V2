{
  "name": "scryvex-pro-monorepo",
  "version": "3.0.0-alpha.5",
  "private": true,
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "typescript": "^7.0.0"
  }
}
