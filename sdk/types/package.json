{
  "name": "@scrypted/types",
  "version": "0.5.55",
  "description": "",
  "main": "dist/index.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "tsc --project src && rimraf dist gen && node -r ../../tools/typescript-compat/register-fallback.cjs ../node_modules/.bin/typedoc && tsc -p tsconfig.build_script.json && node -r ../../tools/typescript-compat/register-fallback.cjs ./src/build.js && tsc && rimraf ./src/build.js"
  },
  "types": "dist/index.d.ts",
  "dependencies": {
    "openai": "^6.1.0"
  }
}
